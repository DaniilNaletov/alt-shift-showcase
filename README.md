# Alt Shift Showcase App

Демонстрационный проект с примером современного frontend-приложения.

**Стек:** React, TypeScript, Tailwind, Axios, React Query, React Router

Архитектура приложения основана на **строгом разделении по слоям и модулям** с чётко определёнными потоками зависимостей.

Demo preview: https://alt-shift-showcase-production.up.railway.app/
UI-kit demo: https://alt-shift-showcase-production.up.railway.app/test-ui

---

## Структура проекта 🗂️

- **`app`** – корневой модуль приложения, содержит глобальные провайдеры, роутинг и точки входа
- **`pages`** – страницы приложения
- **`modules`** – модули продуктовых доменов
- **`shared`** – общие утилиты, не связанные с конкретным приложением (может быть перенесён в любой другой проект без изменений)
- **`services`** – инфраструктурные вещи и сервисы взаимодействия с внешними системами (API, аналитика, Sentry и т.д.)
- **`altui`** – мини дизайн-система, набор переиспользуемых UI-компонентов

### Поток зависимостей ➡️

**`app`** → **`pages`** → **`modules`** → **`altui, shared, services`**

## Структура модулей 🗃️
- **`model`** - модели данных, интерфейсы, хелперы над моделями (*Entities layer*)
- **`components, hooks`** - UI слой (*Presentation layer*)
- **`api`** - взаимодействие с внешними системами (*Infrastructure layer*)
- **`actions`** - бизнес логика модуля (UI/Framework agnostic), взаимодействие с api, side-effects (*Application layer*)

### Поток зависимостей модулей ➡️
**`components, hooks`** → **`actions`** → **`api, model`**



